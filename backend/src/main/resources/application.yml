server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gtchef?createDataBaseIfNotExists=true
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    generate-ddl: true
